[pytest]
addopts = -m "not integration"
markers =
    integration: tests that require a running PostgreSQL instance
    local_socket: test connects to a loopback server it started itself
    pg_env: test deliberately varies a PG* environment variable
