# Resume prompt — PA memory-system (workstream B)

**Written:** 2026-06-06 (session-close `/handoff`). **Purpose:** copy-paste-ready
prompt to resume the personal-assistant memory-system infra work after a break.
Infra work is being **paused for a while** — research (papers; Europe ~24 Jun) is
the priority.

Copy the block below into a new session to resume:

```text
Resume PA workstream-B (memory-system). PA is OUT-OF-HOURS — check tasks/FOCUS.md
first; research (papers; Europe ~24 Jun) is primary. For state read
wiki/continuity.md (the 2026-06-06 "SESSION CAPSTONE" entry at the top of the
session log) + wiki/planning/memory-write-path-plan.md §6a.

Next: the write-path plan is largely closed. The only dated item is the
2026-06-13 §8 review (minutes on the day — leave the digest window undisturbed:
the two dark sentinels OFF, ~/.pa-digest-stage1 ON). Everything substantive else
is gated — item 16 Stage 2 needs months of surfacing data; items 7/10 are
§8-gated; P7 + item 15 are API-gated.

Carry-forward (not obvious from a doc skim):
- item 16 (earned utility) is the forward bet. Anchor coverage (item 6) was
  repriced/closed this session — do NOT resurrect Lever A (deterministic
  inference, 13% net + dilution) or Lever B (type expansion, <3% demand). P3 is
  also CLOSED as superseded — don't resurrect.
- 3 user-observation candidates were accepted 2026-06-06 in
  wiki/user-observations.md (no action needed; noted for continuity).
- The 2 failing lit_search 429-retry tests are a concurrent session's (commit
  fbe743c, lit-search.py) — NOT ours; confirmed pre-existing on clean HEAD.
- Optional non-urgent: archive-memories' own exit(2)-after-commit (minor,
  audit-flagged); P4's deferred CLAUDE.md trims (E1/C1).
```

## What shipped this session (2026-06-06), for quick reference

P8 (`/forget` + `/update` → PostgreSQL), item 16 Stage 1 (earned-utility surfacing
instrumentation, now visible in `/memory-health` [G] + `/weekly-review`), item 8
(`scripts/drift-sweep.py`, [H]), P10 (extraction `max_tokens` 2000→8000 +
truncation salvage), Item C (pre-v2 backup removed), P3 closed-as-superseded,
lit-scout Zotero fix branch merged, item 6 scoped + both deterministic levers
killed by cheap validation, a 5-agent QA pass (0 Critical), and `recover_anchors.py`
brought to crash-safety parity with `archive-memories.py`. All no-API, all on
`origin/main`. Full detail in the 2026-06-06 entries of `wiki/continuity.md`.
