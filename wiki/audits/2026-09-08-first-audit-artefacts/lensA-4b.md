# Lens A (correctness) — tranche 3c: retrieval and serving — HEAD 505ca8c

## Critical
1. fetch-memories.py:552-555 — matched.sort(key=_parse_datetime(created_at)) mixes naive and aware datetimes: _parse_datetime (513-525) returns fromisoformat(cleaned) without normalising a missing offset; its failure branch returns an aware epoch. _timestamps.py:11-14 documents legacy date-only YYYY-MM-DD records still on disk; the hook fixed this at session-start-retrieval.py:452-453; this script did not. JSONL fallback dies with TypeError (exit 1, traceback) — /recall's depth-fetch fails hard exactly when Postgres is down. Same at search_archive (608-611). CONFIRMED (synthetic two-record corpus).
2. fetch-memories.py:541-556 + memory_mcp.py:250-271, 433-445 — matches_filters (462-510) never checks is_active: every JSONL fallback returns forgotten records. commands/forget.md:62-65 asserts the opposite ("that machine's recall reads the git-synced JSONL directly, so the JSONL edit alone suffices"). The `note` fields say "decay rules NOT applied", conflating decay with soft-delete. A retired memory resurfaces on any PG-less machine, any outage, and via MCP search_memories/get_memory. CONFIRMED (is_active:false record returned ranked #1, no marker).
3. commands/recall.md:57-70, 234 — the /recall procedure filters on content/category/tag only; no step mentions is_active; line 234 says "All memories are searched, including decayed categories". /recall is the path forget.md names as the ID source. CONFIRMED (read).
4. project_id.py:56 — str(Path(cwd).resolve()).replace("/", "-") does not encode "."; Claude Code does: live projects dir has -home-shawn-personal-assistant--claude-worktrees-workstream-g-efficacy (double dash) while encode_project_id yields …-.claude-worktrees-…. The module docstring (11-14) exists to prevent exactly this drift. Any cwd with a dot component (every .claude/worktrees/ worktree) → project-scoped retrieval, collect_project_tags, Vector-2c scoping see zero same-project memories. resolve() also breaks a symlinked cwd. CONFIRMED (repro + directory evidence).

## Medium
5. memory_mcp.py (whole file; no surfacing_log import) — six tools serve memories; none logs to surfaced.log. tier-2-retrieval.md:82-92 enumerates "three paths"; MCP is an undeclared fourth. Earned-utility counts under-count MCP retrieval. CONFIRMED (absence).
6. fetch-memories.py:732-757 — if args.semantic and try_semantic returns a list, `results is None` is false so FTS never runs: --semantic X --query Y discards --query; --id discarded on every semantic call (try_semantic has no memory_id parameter); empty semantic result [] never falls back to FTS despite the stderr contract at 739-745. SUSPECTED (would call Ollama).
7. fetch-memories.py:389-390 — WHERE embedding IS NOT NULL drops un-embedded rows entirely rather than ranking last: a memory written since the last backfill is invisible to --semantic and MCP semantic_search (docstring 307-313 silent). CONFIRMED (SQL read).
8. commands/recall.md:174-187 — the session-search snippet tells Claude to substitute user text into plainto_tsquery('english', 'QUERY_HERE') inside a psql -c shell string; a quote breaks the literal; hostile text alters the statement. Every Python caller parameterises. CONFIRMED (read).
9. search-sessions.py:70-86 + no statement_timeout anywhere in scripts/ — --substring with a pattern under three characters cannot use idx_session_chunks_text_trgm, seq-scans session_chunks, materialises all matches before ORDER BY … LIMIT; no connection- or statement-level timeout; MCP exposes it. Given the script's stated purpose (avoiding the 2026-06-21 hard-lock) that is the wrong failure mode. SUSPECTED.

## Low
10. log-recall.py:70-77 — `limit` is the one free-form field not whitespace-collapsed (argparse --limit without type=): format_line("query", "10\tsource=fetch\tresults=999", 3, "recall") emits forged columns ahead of the real ones. CONFIRMED.
11. search-sessions.py:98 — ORDER BY rank DESC, s.started_at DESC NULLS LAST has no final tiebreak (substring mode at :86 adds c.id DESC); FTS ordering non-deterministic. CONFIRMED.
12. search-sessions.py:149-157 — show_turns row-numbers over all rows matching archive_dir; uniqueness is (archive_path, turn_idx), so with --include-subagents the focus CTE can return several rows and the cross join multiplies output. SUSPECTED.
13. resolve_session_id.py:61 — candidate = root / rel with rel unvalidated from CATALOG.json; absolute or ../ escapes the archive root. No containment check. SUSPECTED.
14. resolve_session_id.py:92, 100-111 — docstring promises "Exit 2 for usage / IO errors"; main has no exception handling; a stale-mount OSError from is_dir()/rglob() over ~/mnt/rpi-shares escapes as a traceback, exit 1. CONFIRMED (read).
15. fetch-memories.py:660-702 — format_output never prints the memory id (nor does recall.md's display block), yet forget.md:18 says "Get IDs from /recall" and surfaced.log records ids the operator never saw. CONFIRMED.
16. fetch-memories.py:491-497 — matches_filters(..., tags=[]) returns False for every record (any() over empty). Unreachable from the two callers (normalise [] -> None); trap for a third. CONFIRMED.
17. fetch-memories.py:813 and digest-preview.py:39,181-185 — __file__-derived log paths, no env override, no pytest guard, resolving through logs -> data/logs into the private submodule. surfacing_log.py:97-127 fixed this (S22); siblings not. digest-preview writes by default (--no-log opts out) and digest_log_line carries no preview marker, so a dry run is indistinguishable from a real session digest in digest.log. CONFIRMED (read).
18. memory_mcp.py:102,274,396,479,511,592 — psycopg2 error text returned to the MCP client (socket paths, DB name). No traces, no secrets; host detail crossing the boundary. CONFIRMED.
19. infrastructure-reference.md:118,176 — "5 read-only MCP tools"; the file defines six (search_sessions added later). CONFIRMED.

## Cross-file
- session-start-retrieval.py:1381-1400 (legacy four-bucket path, flag OFF) — retrieve_recent/permanent/middle_aged/constraints apply no is_active filter (only is_disproved, 572-580) and only build_session_digest (1281) calls log_surfaced: with digest mode off, forgotten memories enter session context unlogged. digest-preview.py:61-83 reproduces that path. digest.py:253-259 has the correct is_active helper, unused outside the digest.
- Two archive roots: search-sessions.py reads PG session_chunks (indexed from ~/cc-archives, main transcripts only); resolve_session_id.py:36 defaults to ~/mnt/rpi-shares/cc-archives-consolidated. Not cross-referenced.
- JSONL-fallback ranking diverges: fetch-memories.py:552 parsed datetimes (crashes); memory_mcp.py:264-265 raw string. Undocumented.

## Verified correct
Parameterisation of every Python-issued query; LIKE escaping in search-sessions.py:75-77; is_active on all four PostgreSQL paths (active_memories view, schema.sql:269-281); connections closed on every path; limit bounds; exact-match --id/session_id; empty/punctuation queries; UK English; no embedding call without an explicit flag (embed.py Ollama-only, localhost:11434, 5 s probe; no OpenAI path).

## Answers
1. Forgotten memories returned on four paths: recall.md's procedure (3), fetch-memories JSONL fallback, MCP search_memories/get_memory fallbacks (2), flag-OFF hook path. SQL and vector paths clean.
2. Identical on PostgreSQL (MCP calls fetch_memories.try_postgres; ORDER BY created_at DESC, no tiebreak); divergent on the JSONL fallback; MCP supports `project`, CLI has no flag though try_postgres accepts it.
3. PG down: fetch-memories → JSONL fallback, crashing on mixed timezones; MCP search_memories/get_memory → JSONL fallback (same missing filters); semantic_search/search_sessions/list_recent/memory_statistics → fail closed with an error envelope; search-sessions CLI → exit 2; /recall → always JSONL, psql session search silently skipped. No fallback applies is_active. psycopg2.connect sets no connect_timeout.
4. Neither log-recall nor surfacing_log --ids verifies against rendered output (both model-supplied); the autonomous path logs exactly what format_output printed. Three distinct log files by design; log-recall output invisible to the earned-utility aggregator.
5. search-sessions reads PG session_chunks only (built from ~/cc-archives, main transcripts unless --include-subagents); never live ~/.claude; cannot read mid-write. resolve_session_id points at a different root.
6. __file__-derived live-writing paths: fetch-memories.py:813, digest-preview.py:39; log-recall.py:49 / log-confab-flag.py:69 accept log_path= but expose no CLI flag, so a CLI test writes live. Hermeticity guard watches HOME only.
7. No embedding call without an explicit flag.
