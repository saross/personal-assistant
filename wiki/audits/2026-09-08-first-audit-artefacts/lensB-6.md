# Lens B (test adequacy) — tranche 6: bake-off and style tooling — HEAD 0d5fc39 (unchanged at 5294aba)
Test inventory: ZERO for all three scripts. Baseline 2453 passed; three damaging mutations (M1 seed from time, M2 dry-run falls through to billed calls, M3 the analyser writes into wiki/ and reports/) all green.

## Critical
1. bake-off-metadata.py:1126-1130 — --build-rubric uses a literal replace of "<!--BEGIN-SESSIONS-->\n<!--END-SESSIONS-->"; any separation (blank line, CRLF) no-ops, the unpopulated template is written, "Wrote populated rubric" printed, exit 0, and a fully populated .blind-key.json alongside. CONFIRMED (5-line rubric, zero output blocks).
2. resample-bake-off-manifest.py:550-564 — dedup keeps the WRONG copy: comment (541-547) says archived copies are preferred; sort key (session_id, transcript_path) puts /home/shawn/.claude/… before /home/shawn/cc-archives/… ('.' < 'c'). Every dual-resident session enters as source="live", meta_path=None, three_ps_state="unknown" (:305-315), dropping it from both tiers at :399-408. CONFIRMED.
3. resample-bake-off-manifest.py:527-628 — no dry-run, no confirmation, no path override: PA_DIR hardcoded literal (:52), MANIFEST_PATH derived (:53-56), no argparse, main() unconditionally write_manifest → MANIFEST_PATH.write_text (:616, :518). Any invocation overwrites the canonical manifest; untestable without constant surgery.
4. Hermeticity guard does not watch reports/ or wiki/ (conftest.py:300-305 watches the store and logs/ only): M3 wrote wiki/vocabulary-report.md and reports/vocab-mutation.md and the suite stayed at 2453. CONFIRMED. (Round 4a-2 is widening the guard.)

## Medium
5. bake-off-metadata.py:1054 — zip(("A","B","C","D"), order) truncates a fifth provider silently. CONFIRMED.
6. bake-off-metadata.py:978 — requests[0] on an empty manifest → IndexError exit 1 after dry-run-cost.json was written (:983-986). CONFIRMED.
7. resample-bake-off-manifest.py:427-428 — an under-filled stratum is never reported (:384-386 warns only on a completely empty bin); counts are honest but nothing says a stratum came up short.
8. bake-off-metadata.py:1049-1055 — blinding is two permutations (alphabetical order, per-session % 2 reverse), not 24; deterministic but weak.
9. bake-off-metadata.py:1229 — load_env() runs before the dry-run branch, hydrating .env secrets into os.environ on a no-network run; PA_DIR (:151) is __file__-derived so a test running the real script reads the operator's real .env.

## Low
10. analyse-wiki-vocabulary.py:196 — min(valid_dates) ValueError on an empty/all-malformed corpus; n_all (:215) lacks the max(1, …) guard n_recent has.
11. analyse-wiki-vocabulary.py:149 vs :157 — .lower().strip() vs .lower() only.
12. bake-off-metadata.py:1276-1279 — input() with stdin closed → uncaught EOFError traceback (fails closed).
13. resample-bake-off-manifest.py:470,:492 — datetime.now() twice (midnight straddle).
14. Both scripts importlib-load the extractor re-exported from cc_session_toolkit (editable install at ~/Code/cc-session-toolkit) — out-of-repo, unpinned.

## Minimum suite specification
Shared fixtures (tests/fixtures/): manifest row with exactly the keys write_manifest emits (resample:445-455) and assemble_requests reads (bake-off:279-293); a synthetic 60-line session.jsonl of alternating user/assistant records (distils through the real extractor — verified); response JSON bare, fenced, fenced with trailing prose, {"error": …}; a memories JSONL line with research_tags + created_at, one malformed line, one unparseable created_at.
Boundaries to stub: anthropic.Anthropic (bake-off:459, :494, :880), google.genai.Client (:647), urllib.request.urlopen (:743), resolve_openai_key (:723); roots MEMORIES_JSONL (vocab:53-56), PA_DIR/MANIFEST_PATH (resample:52-56), PA_DIR/ENV_FILE (bake-off:151-152) — all module constants, none CLI-overridable.
bake-off-metadata.py: (1) dry run writes exactly <out>/<provider>/dry-run-cost.json and constructs no client (:983-986, :1259-1261) — kills M2; (2) --build-rubric with a blank line between markers FAILS LOUDLY (:1126-1130); (3) every discovered provider in the rubric incl. a fifth (:1054); (4) blind key byte-identical across runs; mapping never in the rubric body (:1049-1055, :1137-1148); (5) {"error"} response redacted in the rubric, preserved in the key (:1064-1075); (6) parse_response_json round-trips bare/fenced, ValueError on prose (:394-414); (7) estimate_cost_usd includes SYSTEM_PROMPT_TOKENS_APPROX once per request in aggregate and per-session rows (:328-331, :367-370); (8) NEGATIVE: stdin "no" → Aborted, exit 0, no calls (:1276-1281); --yes proceeds; --dry-run never reaches the gate; (9) NEGATIVE: haiku_apply with a custom_id absent from batch-state.json writes nothing for it (:512-514); (10) entry point: --dry-run + one-row manifest → exit 0 + file; EMPTY manifest → clean exit with message, not IndexError (:978); --haiku-apply without --provider haiku → exit 2 nothing written (:1245-1248); --build-rubric without --rubric-in/out → exit 2 (:1231-1234).
resample-bake-off-manifest.py: (1) same seed → byte-identical manifest except generated_at/notes (freeze datetime.now) — kills M1; (2) dedup prefers the archive copy (:550-564) — FAILS today; (3) classify_bin boundaries 999→None, 1000/49_999 short, 50_000/119_999 medium, 120_000/190_000 long, 190_001→None (:62-74, :318-323); (4) LFS pointer stubs skipped (:135-141); sub-MIN_TOKENS dropped (:340-341); (5) enumeration order-independent (:227, :270); (6) NEGATIVE: a bin with 1 candidate vs target 3 → 1 pick, honest counts, AND an explicit shortfall message (fails today at :427-428); (7) NEGATIVE: nothing written outside the redirected MANIFEST_PATH (:518); (8) entry point via monkeypatched MANIFEST_PATH/ARCHIVE_GLOBS/LIVE_GLOBS — RECOMMEND adding --out and --dry-run flags.
analyse-wiki-vocabulary.py: (1) NEGATIVE: writes nothing anywhere (snapshot the tree before/after main()) — kills M3; (2) --as-of pins the window; byte-identical output (:190-193); (3) load_memories skips malformed lines (:127-130); (4) wiki_tag_support counts memories, tag_frequencies counts usages: a record with two matching tags → 1 and 2 (:144-150, :153-163); (5) all 24 WIKI_TAG_EXPANSIONS keys appear even at zero support (:216-218); (6) entry point on an empty/all-malformed corpus exits with a diagnostic, not ValueError (:196).
