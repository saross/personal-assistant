# Lens B (test adequacy) — tranche 8: scripts/style-analyser/ — HEAD 2a4b3bc (unchanged at 0bed06c)
Baseline 2453 passed; 7 damaging mutations all survived (passive always false; mattr constant; judgements paired by index; promote everything attested; pairing broken topic i vs n-i; guide-side key appended to the judge's reference.md; split_body_references returns all text as body).

## Critical
C1. Zero tests for the tranche (5,546 lines, 14 scripts); no sweep test collects scripts/style-analyser/.
C2. The two validate_*.py scripts are one-off human-audit printers: not collected, no assertions, `return 0` unconditionally (validate_passive_detection.py:123, validate_announce_colon.py:244).
C3. phase5_evaluator.py:948/:670-673/:1007/:989-991/:1015-1017 — the "held-out real" sanity paper is scored against X fit on all 18 papers including itself; the verdict is near-tautological.
C4. efficacy_build_judge_tasks.py:129 — judge-mapping.json written into JUDGE_DIR beside reference.md (:104) and pairNN_A/B.md (:119-120); the mapping records condition-revealing source filenames (:125).
C5. phase1_pipeline.aggregate (:582-590) statistics.mean over per_paper; main() skips missing files (:749-751) then aggregate([]) at :770 → StatisticsError.
C6. phase3_guide_verifier.py:532-533 drops any § not in SECTION_TO_METRICS (:41-60, 14 sections) against 52 §-claims at phase3_promotion.py:331-343; extract_per_1k_aggregates (:205-211) never called → a confabulated per-1k rate in §6.2/§6.3 passes with "✓ PASS" (:558), exit 0 (:586).
C7. numpy/scipy/sklearn/spacy absent from requirements.txt and venv/: phase5_evaluator, efficacy_score, efficacy_build_reference fail on import numpy; extract_corpus sys.exit()s at import (:51-54) when ~/Code/llm-reproducibility is missing; validate_passive_detection needs spaCy. The other 8 modules import cleanly on stdlib.

## Medium
M1. phase3_guide_verifier.py:277 `elif valid and not out[-1:] or (...)` precedence; with p3_for_section empty a declared Status emits NO row (:268 guard) → an unbacked status invisible rather than FAIL. SUSPECTED.
M2. Report not byte-reproducible: list(set(...)) at :196; FAIL rows :419-426 in set order (five runs, five orderings).
M3. No dry-run anywhere; relative/hardcoded production paths (phase3_promotion.py:38-39, phase4_exemplar_scorer.py:27,249, phase5_evaluator.py:109-111, phase3_guide_verifier.py:520-521); phase4 from an arbitrary cwd wrote data/style-corpus/phase4-exemplar-candidates.json into that cwd. (Path defaults are being changed on branch claude/audit-round4e.)
M4. efficacy_build_prompts.py:256 creates the experiment directory before :202 reads the guide → partial side effect on failure.
M5. efficacy_build_judge_tasks.py:85-86 rmtree(JUDGE_DIR) every run, no guard; destroys judgments.jsonl.
M6. efficacy_score_judges.py:51 KeyError on unknown pair_id; :87 ZeroDivisionError on empty file; :66 hardcodes ["A1","A2","B1","B3"] so a missing topic prints 0/0 as if measured.
M7. extract_corpus.py:702 manifest to args.output_dir.parent; :649 --keys bypasses the EXCLUDED filter without --include-excluded.

## Low
L1 validate_announce_colon.py:241 hard-codes "n=3". L2 :213-215 missing phase1-results.json → reported=0.0 → "corrected 0.000/1k" indistinguishable from a measurement. L3 :179 unguarded read; both validators hardcode /tmp/style-corpus-extract and fixed keys. L4 validate_passive docstring claims exact mirroring but its sentence set is raw spaCy, not phase 1's 5-200-word splitter (:229). L5 unused import; min()/max() on possibly-empty lists (phase5_evaluator.py:408-409, efficacy_build_reference.py:165). L6 (outside tranche) tests/test_daily_sync_trigger.py not midnight-safe (two failures only in the run that straddled 00:00).

## Validator verdicts (sandbox, synthetic fixture)
validate_announce_colon: 1 match, TRUE ANNOUNCEMENT COLON; decoys 10:30 and 3:1 excluded by the lookbehind (phase1_pipeline.py:413); reference stripping "header". validate_passive_detection: flagged exactly the 3 nsubjpass/auxpass sentences (2 true passives + the predicative-adjective case); prints VERDICT: ___ for a human. Neither wrote anything.

## Minimum suite specification
Stub boundaries: (a) the spaCy nlp object — every consumer takes it as a parameter (phase1_pipeline.spacy_features:348, phase5_evaluator.evaluate_text:643, efficacy_build_reference.sentence_windows:66) → inject a fake nlp returning a scripted doc; (b) filesystem roots — phase3_promotion.PHASE1/OUT (:38-39), phase4_exemplar_scorer.CORPUS (:27) and :249, phase5_evaluator.*_DEFAULT (:109-111), REPO_ROOT-anchored EXP/EXTRACTED in the efficacy scripts. Add numpy/scipy/scikit-learn/spacy + en_core_web_sm==3.8.0 to requirements, or mark the four heavy modules' tests integration/importorskip.
Shared fixtures: (i) corpus_text — 8 sentences (6 clear the 5-200-word filter), 2 true passives + 1 predicative false positive, exactly 1 announcement colon, 2 colon decoys, 1 hapax, 1 first-plural, 1 concession (However), 1 "; ", plus a "References\n\nSmith, A. B. (1999) …" tail; (ii) phase1_results.json — 6 synthetic papers with exactly the dotted paths build_corpus_matrix reads (phase5_evaluator.py:123-139) plus aggregate.regression.*, aggregate.sentence_stats.mean, aggregate.hedge_per_100w, aggregate.concession_rate, aggregate.n_words — one bimodal, one with a None feature; (iii) phase3_promotion.json with one bimodal: true metric; (iv) scores.json 3 topics × 3 conditions × 2 reps, one topic missing C1 (efficacy_analyse.py:173-175); (v) judge-mapping.json + judgments.jsonl in four shapes (well-formed, blank-padded, unknown pair_id, empty).
phase1_pipeline: strip_references "header" only for a tail-position match (:146-148), "none" for an early link; author-year-density thresholds (:152,161,167); split_sentences drops <5 and >200 words, paragraph-aware (:229,:223); _is_prose_block (:261-279); mattr_100 short-circuit (:294-295) and hand-computed mean; hapax_ratio (:303-308); announcement_colons 1 on fixture, 0 on decoys (:413); hedge_density word boundaries (:425-428); concession_rate once per sentence (:456-468); regression_counters U+2014 only (:485); included_keys case-insensitive exclude (:687-695); regression_report exact anchors ±1 → main returns 1 (:647-666, :792-799). Negatives: empty corpus → diagnostic + non-zero, not StatisticsError; byte-identical re-run.
phase3_promotion: five verdict branches at exact boundaries (:132-175); detect_bimodal ≥6 papers, ≥3 per side (:69,79-81), False for a single outlier; papers_present sorted desc, papers_absent sorted (:120-126). Negatives: all-zero metric never promotes above absent-when-searched-candidate (kills M4); byte-identical; injected out-path.
phase3_guide_verifier: split_claim_blocks terminates at ###/## (:156-173); checks 1-7 each FAIL (:240-510); main returns 1 iff any FAIL (:586). Negatives: a claim in a section absent from SECTION_TO_METRICS reported as unverified (C6); a per-1k claim contradicting phase 1 FAILs (currently dead code); bytes stable under PYTHONHASHSEED=0 and =1 (M2); a verifier-FAIL claim never promoted.
phase4_exemplar_scorer: score_sentence once per category, em_dash dropped post-2022 (:117-118); is_pre_2023 default True on unparsable date (:185-190); 20-80-word window (:220); MIN_CATS/TOP_PER_PAPER deterministic tie-break (:226-227); missing corpus exit 2 (:200-202). Negatives: byte-identical; writes only under an injected root.
phase5_evaluator: resolve_feature_space moves exactly bimodal metrics to advisory, keeps metric=None candidates (:213-232); build_corpus_matrix raises on non-numeric (:248-251); standardiser zero-variance → 1.0 (:263); leave_one_out refits per fold excluding the row (:287-292); classify_envelope at loo_max and 1.5×loo_max (:330-341); GateCheck.evaluate three modes at the edge (:433-441); build_gate default one-sided em-dash ceiling, --corpus-em-dash legacy band (:511-533); evaluate_gate all eight actuals (:594-603); main returns 1 on gate fail (:1158). Negatives: off-register fixture beyond loo_max (SANITY_FIXTURES:872); the held-out paper actually held out (C3); <200-word input sets short_input (:700).
efficacy_* (7): strip_citations year-parenthetical removed, (FAIR) kept (efficacy_build_prompts.py:52-63); _marker_line named error (:186-191); C0/C1 no guide text, C2 has it (:112-131); sentence_windows drops a trailing window below min_words (efficacy_build_reference.py:88); mid_window clamps (efficacy_build_judge_tasks.py:76); both orders with guide_side correct (:114-126); FNAME_RE accepts A1__C2__rep1, rejects malformed into skipped (efficacy_score.py:41,112-115); excerpt matrix but whole-corpus gate (:62-65); judgements join on pair_id, picked_guide == (choice == guide_side) (efficacy_score_judges.py:51-53); exact_signflip_p enumerates 2^n, 1/16 for n=4 all-positive (efficacy_analyse.py:88-97), drops exact zeros (:79,87); paired_block strictly within topic (:120-124), None effect size when loo_sd == 0 (:141-142). Negatives in priority order: shuffling the treatment column changes the p-value (kills M5); the blinding key lives outside the judge's read root with no condition-bearing filenames (kills M6/C4); empty/malformed judgments.jsonl is a diagnostic (M6); refuse rmtree when judgments.jsonl exists (M5); every writer --dry-run with zero bytes (M3); byte-identical re-runs.
extract_corpus: replace the import-time sys.exit (:51-54) with a lazy import inside extract_one; pin the five split detectors in order and the fallback (:374-412) on six Markdown fixtures; strip_running_headers at exactly min_occurrences (:189); apply_manifest_overrides (:165-169); each compute_qa_flags flag at threshold incl. expects_refs=False suppressing references_split_failed (:457-461); --keys matching nothing → exit 2 (:650-655); main returns 1 iff any error (:712). Negatives: references never silently retained (kills M7); corpus-manifest.json inside --output-dir; --keys on an EXCLUDED paper refused without --include-excluded.
