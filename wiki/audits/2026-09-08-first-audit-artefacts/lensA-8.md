# Lens A (correctness) — tranche 8: scripts/style-analyser/ (14 files) — HEAD 2a4b3bc (unchanged at 73c4050)

## Critical
1. efficacy_score_judges.py:54 — `"picked_guide": j["choice"] == m["guide_side"]` scores any non-matching choice (tie, refusal, "") as a baseline win; biases the headline toward "plain" by the number of unusable judgements. CONFIRMED (tie + "" → 1/3, exit 0).
2. efficacy_score_judges.py:47 — json.loads(line) with no fence-stripping, schema check, or error handling: a ```json fence or a prose refusal aborts with JSONDecodeError; empty file → ZeroDivisionError at :87; a duplicated pair_id counted twice (2/2 from one judgement); a missing pair silently dropped. CONFIRMED.
3. efficacy_build_judge_tasks.py:129 — the unblinding key judge-mapping.json (guide_side, A_source, B_source) is written INTO judge-tasks/, the directory handed to the judge; the docstring says the judges never see it. CONFIRMED.
4. phase3_guide_verifier.py:394-403 — the `count / words` check matches the numerator against ANY integer in aggregate.regression, not the claimed metric: "73 semicolons / 127,720 words" passes because 73 is the em-dash count. CONFIRMED.
5. phase1_pipeline.py:303-308 — hapax_ratio divides by len(words) (tokens) while its docstring and efficacy_build_reference.py:8-9 define it over types; 6 tokens/4 types/3 hapax → 0.5 not 0.75; the length artefact the length-matched reference exists to correct is larger than assumed. CONFIRMED.

## Medium
6. phase3_guide_verifier.py:294-337 — sub-cluster keyword list (low/high/range/bin/tier) matched against a ±60-char snippet downgrades a genuine numeric mismatch from FAIL to WARN on an incidental word. CONFIRMED.
7. phase3_guide_verifier.py:196,417-426 — `\b([0-9A-Z]{8})\b` treats any 8-char upper-case token as a Zotero key (METADATA, ANALYSIS, 20260530 → spurious FAILs in check 5; check 6 filters to known keys at :455). CONFIRMED.
8. phase1_pipeline.py:383-396 — passive counted per qualifying VERB (ratio unbounded above 1) while v2.md:253 specifies presence per sentence; the published 0.31 is not a fraction of sentences. CONFIRMED.
9. phase1_pipeline.py:397 — nominalisation_per_1000w divides by token_count (includes punctuation) while every other per-1k rate divides by alphabetic tokenize_words(): ~15-20 % larger denominator; 38.6/1k not comparable to 6.54/1k. CONFIRMED.
10. efficacy_build_reference.py:137,146 — windows accumulated to --target-words on citation-INCLUSIVE text, citations stripped afterwards: excerpts land ~14 % short of the 400-word generations; min-words=250 guard applied pre-strip. CONFIRMED.
11. phase5_evaluator.py:948,1002-1012 — the "held-out real" sanity fixture is scored against X built from ALL corpus papers including itself; that arm cannot fail. SUSPECTED (sklearn unavailable in sandbox).
12. phase5_evaluator.py:1019-1021 — a fixture rendered "NOT farther ✗" still leaves sanity_ok True unless also below the LOO median; footer can say PASS over a table saying otherwise. CONFIRMED by inspection.
13. efficacy_score_judges.py:36-39,83-89 — the two counterbalanced orders of one pair tallied as two independent trials ("x/16"), no binomial test/CI/clustering; :83/:88 hard-code "(2 contrasts x 4 topics x 2 orders)" and "mild B-lean" regardless of data. CONFIRMED.
14. validate_announce_colon.py:38,179 and validate_passive_detection.py:30,89 — both read /tmp/style-corpus-extract/<key>.txt (gone; superseded by data/style-corpus/extracted/<key>/body.md); announce validator dies with FileNotFoundError; passive validator degrades silently to zero examples; neither precision estimate can be re-derived. CONFIRMED.
15. phase1_pipeline.py:106-148 — _REF_HEADER_RE needs only a line beginning References/Bibliography in the last 35 %, no heading marker, no EOL anchor: "References to earlier work are given throughout…" truncates the document; process_paper runs it on already-clean body.md (:518) and generated passages. CONFIRMED.

## Low
16. efficacy_build_prompts.py:55,62 — _FIX_DOUBLE_SPACE_RE collapses nested-list indentation and code blocks in the injected guide. 17. :52-53 strip_citations leaves integrated Smith (2012), removes "(Bulgaria in 2019)", leaves "(see)" on nested parens. 18. phase1_pipeline.py:413 _ANNOUNCE_COLON_RE requires [A-Z] after the colon (curly quote scores zero; "In 2019: The" counted, which validate_announce_colon.py:122 classifies as URL/TIME/RATIO). 19. :193-198 no NFC/NFD normalisation. 20. :292-295 mattr_100 silently returns TTR under 100 words. 21. :687-695 / extract_corpus.py:640-642 `"exclude" in notes` matches "not excluded". 22. phase4_exemplar_scorer.py:152 " ".join flattens the document before sentence splitting (cross-paragraph exemplars). 23. :235 n_categories 17 vs documented 18; phase1:75 "14 lemma pairs" vs 18. 24. phase1:390-393 nominalisation suffix scan fires on nation/station/science/… (documented). 25. phase3_promotion.py:119 length mismatch silently disarms the confabulation guard; :378 renders n_occ=0 as "?". 26. :109,132 n_present counts rate > 0 so always-positive continuous metrics auto-promote to attested. 27. efficacy_analyse.py:209-213 feature_profile includes topics excluded from the pairing. 28. phase3_guide_verifier.py:205-211 extract_per_1k_aggregates never called; STATUS_OVERRIDE_ALLOWLIST["3.4"] unreachable (:532). 29. :277 elif precedence correct by accident. SUSPECTED. 30. phase5_evaluator.py:559-564 absolute thresholds on 400-word passages. SUSPECTED. 31. efficacy_build_judge_tasks.py:86 shutil.rmtree(JUDGE_DIR) destroys collected judgments.jsonl on re-run. 32. extract_corpus.py:702 corpus-manifest.json written to output_dir.parent. SUSPECTED. 33. phase1:229 sentences outside 5-200 words dropped from sentence stats while their words count in n_words (documented).

## Cross-file
- Three definitions of "passive" (phase1 per verb; validate_passive per flagged sentence; phase4 surface regex); two of "announcement colon" (phase1 vs phase4; the validator imports phase1's).
- No atomic write anywhere (20+ outputs, zero os.replace); an interrupted run leaves truncated JSON read unvalidated by the next stage.
- No provenance in any output: no hashlib, no git commit, no spaCy model version (warned to stderr only), no judge model id, no prompt hash; extract_corpus records PyMuPDF/pdfplumber versions but not the external extractor's revision (~/Code/llm-reproducibility).

## Verified correct
MATTR window enumeration; LOO distances refit per fold (genuine LOO); standardiser zero-variance guard; exact sign-flip enumeration correct incl. min p 1/2**n; pairing by topic_id with incomplete topics excluded; division guards; sorted() iteration everywhere; no subprocess/shell/eval/pickle/yaml; no .env access.

## Answers
1. External calls: none in all 14 files (generation was done by in-CC subagents outside these scripts; pre-registration §4).
2. Not reproducible end to end: scoring/analysis deterministic from scores.json, but no prompt hash, guide hash (path only, efficacy_build_prompts.py:234), judge model id or prompt file, spaCy version, git commit; efficacy_score.py --reference-phase1 defaults to None so a bare re-run reproduces the whole-paper feature space the pilot rejected (only scores.json["mahalanobis_reference"] records which was used).
3. No default output leaves data/ (reports/, notes/, tasks/ are symlinks) except extract_corpus.py:702 (output_dir.parent) and the relative defaults (phase4-exemplar-candidates.json holds verbatim corpus sentences — privacy-relevant).
4. Validators: announce imports the pipeline regex (agrees) but misclassifies "In 2019: The"; passive counts sentences against a per-verb metric; both read a stale /tmp layout.
5. Promotion cannot override the verifier; the verifier (phase3_guide_verifier.py:246-253) accepts a declared status matching ANY metric in a multi-metric section; the gate is advisory.
6. Blinding leaks three ways: the key in the judge directory; pair ids encode the key deterministically (order alternates, even pairNN always guide-as-A, :114-124, no RNG); each unordered pair emitted twice as byte-identical files.
7. Paths: relative (4e's scope) phase3_promotion.py:38-39 (no override), phase3_guide_verifier.py:520-521, phase4_exemplar_scorer.py:27,249 (no override), phase5_evaluator.py:109-111; __file__ parents[2] in all efficacy_*.py (correct); Path.home() extract_corpus.py:51; hard-coded /tmp in both validators (stale).
