# Round 4a-3 report (branch claude/audit-round4a-3; 8 commits 7456387..057b345)
M2 _sync_cursor.py:867 read_jsonl_lines() reads bytes and decodes; used at sync-to-postgres.py:1358 — 3 tests incl. the real cycle. M3 test over [], "3", null, 3, true, list. M4 conftest.py:258-306 perimeter stated exactly (Python-level TCP connect/connect_ex/create_connection + UDP sendto/sendmsg with destination; gaps: _socket.socket(), children, psycopg2 C-level — covered by PGHOST dead end, stub psql, local remotes) — 4 tests. M5 conftest.py:207 pg_env_unchanged autouse function-scoped: snapshots PG*, restores unconditionally, flags the offender; @pytest.mark.pg_env exempts the comparison — 3 tests (monkeypatch.setenv on PG* alone is not sufficient; marker is the route). M6 pops pinned. Lows: loopback exactly 127.0.0.1/::1/localhost; teardown resets the opt-in; empty directories caught via (0,0) sentinel; tag-gardening.py:65 legacy fallback only when it exists; one behavioural check per watched tree (8 params); pa-test-home atexit cleanup + 24 h sweep (4 tests). Four surviving mutations killed (corpus fsync by inode; logger.error→debug ×2; BaseException; kept_lower.add). Flake: the rig puts a stub `date` first on PATH answering `date +%Y-%m-%d` from a file it owns (fixed 2031 date), delegating the rest — test_a_session_after_local_midnight_syncs_again (crosses midnight and a year), test_the_rig_pins_the_shell_date.
Addendum: store files and logs/ — an append is verified (old byte count re-hashed against a session-start digest) and tolerated; shrink/rewritten prefix/deletion/new file fail in both modes. Source trees (wiki, scripts, hooks, commands, global-claude-md, global-agent-guidance, tasks, tests) advisory: stderr warning, continue; PA_HERMETICITY_STRICT=1 makes it fatal — 8 tests; documented in the conftest docstring and a new commands/audit.md section with the clean-copy invocation.
Collection-count difference: not reproducible (2564 selected both ways); the earlier gap was different commits.
Suite (env -i, HOME pinned, PA_HERMETICITY_STRICT=1): 2571 passed, 1 skipped, 3 deselected, exit 0. Two environmental failures recorded: /tmp filled to 100 %; pytest's /tmp/pytest-of-shawn numbered-dir namespace collided with a sibling agent (505 errors) — a cross-agent collision on a fixed path; used --basetemp inside the pinned HOME.
Live risks: PA_HERMETICITY_STRICT is the contract for audit runs (documented; nothing enforces it); a NEW file under logs/ still fails in both modes (a hook creating a log mid-run would be a false failure); the session-start sweep deletes pa-test-home-* dirs older than 24 h; UDP needs the marker; PG* changes need @pytest.mark.pg_env; the opt-in does not survive into teardown; sync-to-postgres reads bytes (identical today: 42,860 both ways, zero raw separators).
4a-4 (6904195): M1 pytest_terminal_summary (conftest.py:1119) writes the advisory and the append note under a "hermeticity" banner; fixture teardown only queues into _DEFERRED_REPORT; nested-pytest tests grep stdout under default capture (advisory reaches the operator; fatal under STRICT). M2 commands/audit.md:139 two invocations (clean copy with the live venv — store half INERT, says so via strict_store_coverage_warning() at conftest.py:1005; live/populated worktree under STRICT in a quiet window); --basetemp explained. M3 session-order pair test. M4 verify appends: _appended_content_problem (conftest.py:930) requires an appended memories.jsonl line to parse with id/content/created_at (_REQUIRED_MEMORY_KEYS) and a vocabulary line to be a bare tag; tolerated appends printed with byte counts (5 tests). L1 message. L2 hash once (with_digests=False at teardown); found and fixed: a tuple comparison would have marked every store file changed on every run in a populated checkout — entries compare on (mtime, size) via comparable(). L3 _shared_checkout_noise tolerates *.lock creation and a rotation in advisory mode, fatal under STRICT (4 tests). L4 tests/ added to _CANONICAL_DIRS (9 entries). L5 five mutations killed. Suite clean copy env -i STRICT: 2918 passed, 1 skipped, exit 0 (store half inert there, banner shown); worktree 2919. Gap: the store half has never run strict against a populated store. Risks: the M4 shape check couples the guard to the hook's record keys; tests/ watched advisory; the advisory banner is new output (named data/logs/daily-sync.lock +0 bytes from the bulk-rewrite guard).
4a-5 (86051f6, ece0ba3): 1 three nested-pytest tests under default capture grep the child's stdout for the INERT banner, the append note (+N bytes, populated synthetic store), and the tolerated-noise note — kill the wiring mutations. 2 the teardown classifies once and passes classified= to both halves (conftest.py:1380); behavioural + structural tests. 3 _line_problem compares stripped != line (indented/trailing-space vocabulary appends are violations). 4 compressed rotations (.gz/.bz2/.xz/.zst/.Z) tolerated in advisory mode; a new logs/*.log tolerated in advisory mode; a new non-.log file under logs/ still a violation; all fatal under STRICT. 5 a clean append whose final line lacks a newline → tolerated (reported), fatal under STRICT; a garbage complete line before it stays fatal. 6 both halves collected, every note queued, one failure naming both. 7 the banner lists the paths actually missing and both causes. Self-caught: a test that drove report_source_tree_changes left its path in the module-level _DEFERRED_REPORT, so the session summary cried wolf; an isolated_report fixture now scopes it. Suites 3694 (worktree) / 3693 + 1 skipped (clean copy), exit 0; STRICT inert in both (no populated store).
Risks: the store half still has no full-suite run against a real populated store (do the live-checkout strict run after merge); the advisory surface widened (reported, never silent); _DEFERRED_REPORT is shared mutable state (a future direct caller must take the fixture); the partial-line allowance accepts one unterminated garbage fragment in advisory mode (reported; fatal under STRICT).
