# Round 4b fix agent report (branch claude/audit-round4b; 17 commits 2b46da7..963e8e1)
R1 fixed fetch-memories.py:530 _parse_datetime naive->UTC — TestParseDatetime::test_fallback_sorts_mixed_stamps_newest_first
R2 fixed fetch-memories.py:487 is_active() + guard in matches_filters; memory_mcp.py:471 get_memory scan; hooks/session-start-retrieval.py:613,669,732,815 — test_forgotten_record_never_matches, test_all_four_buckets_skip_forgotten, test_search_memories_fallback_drops_forgotten
R3 fixed commands/recall.md:59-64 mandatory filter step, note at :242; commands/forget.md:66-75 claim made true, path list added
R4 fixed project_id.py:45,102 every non-alnum -> "-" — test_encode_matches_pre_batch_inline (live double-dash), test_encode_rewrites_every_non_alphanumeric; verified against all 20 live ~/.claude/projects names (15 whose cwd exists reproduce byte-for-byte)
R5 fixed memory_mcp.py:94 _log_surfaced wired into 4 tools; path=mcp in surfacing_log.VALID_PATHS and surfacing_stats.ACTIVE_PATHS; docs — TestSurfacingInstrumentation
R6 fixed --semantic + --query/--id is a usage error; empty semantic result falls through to FTS
R7 fixed try_semantic(stats=...) counts un-embedded active rows on the same connection; CLI stderr, MCP envelope, tool description
R8 fixed recall.md:183-200 calls search-sessions.py with the query as an argument; remaining psql block is a fixed literal
R9 fixed search-sessions.py:52-72 connect_timeout=5 + statement_timeout=30s + 3-char trigram floor; same in memory_mcp._pg_connect and fetch-memories.py (scope extension)
R10 log-recall --limit type=int + collapsed; R11 search-sessions.py:137 c.id DESC; R12 NOT fixed (SUSPECTED, not in must-fix list; a test pins --archive-path disambiguation); R13 resolve_session_id.py:60,67 containment; R14 :118 OSError -> exit 2; R15 fetch-memories.py:729 ID: line + recall.md display blocks; R16 `if tags:`; R17 default_log_path() in fetch-memories, log-recall, log-confab-flag, digest-preview (own digest-preview.log with preview=true) — tests/test_log_destinations.py (24); R18 generic envelopes at six sites, detail to logger; R19 infrastructure-reference six tools
RT1-3, RT8, RT14 tests/_fake_pg.py + tests/test_fetch_memories_pg.py (five clause mutations killed); RT4 TestMainEndToEnd; RT5/6 TestSearchSessionsTool + tests/test_search_sessions.py (23); RT7 TestJsonlFallbackBehaviour (no stub), MCP fallback ranking converged on _parse_datetime; RT9-13, RT15-18 in b1509fa, 2ee7394, b72b174, ed133b3, 963e8e1, 6cf1efc
NEW: project_id decode docstring example was false (hub id is lossy) — corrected and pinned; fetch-memories.py:226,378 had no timeouts (fixed with R9); session-start-retrieval.py:1383-1420 legacy path still logs nothing surfaced — NOT fixed deliberately (would add a new row class to the earned-utility evidence base; Shawn's call); two archive roots recorded.
Suite 2564 passed, 3 deselected, exit 0.
Live risks: project-id encoding now emits --claude- where it emitted -.claude- (reader now agrees with the writer; spot-check /recall scoping in a .claude/worktrees session); MCP results write to surfaced.log under path=mcp; /recall shows ids and filters is_active; digest-preview writes its own log; --semantic with --query/--id exits 2; --substring under three chars exits 2 / error envelope.
