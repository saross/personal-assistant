# Lens A (correctness) — tranche 5: external services and machine glue — HEAD 0d5fc39

## Critical
1. lit-scout-zotero-import.py:894 — find_existing_by_doi is `LOWER(idv.value) = LOWER(?)` with no URL/scheme normalisation, while zotero.py:328-372 find_by_doi strips https://doi.org/, dx.doi.org, doi:. This weaker function is the ONLY duplicate guard on both write paths (importer --live and add-doi-to-zotero.py --live). A stored `https://doi.org/10.1234/ABC-def` (the connector's form) is invisible to a bare-DOI lookup → duplicate created; add-doi's safety claim (header 20-23) is false. CONFIRMED (synthetic library: find_by_doi 1 hit, find_existing_by_doi 0).

## Medium
2. lit-scout-zotero-import.py:463-477 load_env — no quote stripping, no `export` prefix: `ZOTERO_LIBRARY_ID="1234567"` loads with quotes; `export X=…` sets a variable named `export X`. Fails safe but env-fingerprint.sh reports the same file healthy. CONFIRMED.
3. lit-search.py:203 — `headers["x-api-key"] = S2_API_KEY` set client-wide on the client used for crossref, openalex, datacite with follow_redirects=True (205-209); the docstring above (178-186) states the per-request rule this breaks. Semantic Scholar credential disclosed to three unrelated operators on every call. CONFIRMED.
4. lit-scout-zotero-import.py:1117 — CrossRef `"date-parts": [[null]]` passes `if parts and parts[0]` ([None] truthy) → date == 'None' literal written to Zotero on --live. CONFIRMED.
5. lit-scout-zotero-import.py:1099 — title written verbatim while the abstract (1177-1179) is HTML-stripped; `<i>…</i>` and `&amp;` land in Zotero; zotero.py:168 _strip_html exists unused here. CONFIRMED.
6. add-doi-to-zotero.py:139 — reads only ZOTERO_API_KEY_PERSONAL, no ZOTERO_API_KEY_ALL fallback; zotero-reference.md:175 marks PERSONAL a retirement candidate once the importer is repointed (it is: importer 1353-1354). Breaks when the key is revoked. SUSPECTED.
7. publish-dashboard.py:479 — urllib urlopen with Authorization: Bearer <SLACK_BOT_TOKEN> (471): HTTPRedirectHandler strips only content-length/type, so a 301/302/303 is followed to any host with the token (POST→GET). CONFIRMED from stdlib 3.13.3 source. Same exposure for any future _http_retry.urlopen_with_retry caller with an auth header.
8. lit-scout-zotero-import.py:1277 — ensure_subcollection uses zot.collections_sub(parent_key), one un-paginated page (pyzotero 1.11.1 verified); past one page an existing subcollection is missed and a same-named twin created, contradicting "Idempotent". SUSPECTED.
9. sync-symlinks.sh:243 — `pip install --quiet --upgrade -r requirements.txt` unattended (SessionStart, end of daily-sync) whenever any of six probe imports is missing; --upgrade re-resolves the whole file. SUSPECTED (not executed).
10. sync-symlinks.sh:133 — `git submodule update --init --recursive --quiet` at every SessionStart: on an initialised submodule this checks out the gitlink SHA, detaching data/ from its branch; commits made inside data/ by a concurrent session and not yet pointed at become unreferenced. tests/test_glue_scripts.py:111 guards commit-data.sh against this; nothing guards this path. SUSPECTED (destructive, not executed).
11. compose-global-claude-md.sh:25-30 — sources are $SCRIPT_DIR-derived but TARGET is $HOME/.claude/CLAUDE.md: run from a worktree it overwrites the live global instructions with the branch's content. CONFIRMED in sandbox (16,819-byte CLAUDE.md written into the pinned home from the sandbox's sources).

## Low
12. zotero.py:764 `year = item.get("date", "n.d.")` dead default (date always "" when absent) → 'Smith () A study'. CONFIRMED.
13. compose-global-claude-md.sh:33 — only the exact `--dry-run` spelling; `--dryrun` overwrote the target with no error and no backup. CONFIRMED.
14. _http_retry.py:249 — POST retried on 500/502/504 with no idempotency key; only embed.py calls it today; docstring silent. CONFIRMED.
15. syncthing-health.sh:67 — `--simulate-need) SIMULATE_NEED="$2"` unbounded under set -u; trailing flag → exit 1, breaking "Always exits 0". CONFIRMED.
16. syncthing-health.sh:99,251,288,355,383 — $EXPECTED_FILE/$STATE_FILE/$label interpolated into python3 -c string literals (Python injection via SYNCTHING_EXPECTED_FILE); run_on (:122) builds bash -c "$*" / ssh "$*" with unquoted $container/$config_dir from JSON. SUSPECTED (inputs trusted today).
17. review-paper-prepass.py:102 GUARD_ANCHOR allows `..` and leading `/`; check_guard_anchors:443-457 does repo / anchor then read_text — a comment in a .tex can probe files outside --repo (existence and line count only). SUSPECTED.
18. lit-search.py:1214 cmd_bibtex — no BibTeX key-collision check. SUSPECTED.
19. lit-search.py:622 _normalise_crossref — `elif family:` never reads author["name"]: organisational authors vanish; the importer's _creators_from_record (963-976) handles both.
20. ollama-endpoint.sh:56 — producer not fixed: prints an empty line and exits 1, and in `OLLAMA_BASE_URL=$(…) cmd` the status is cmd's, so the failure is discarded; only the consumer (embed.py:50) is guarded. Documented.
21. llm-use-inventory.py:369 — `root / args.project` unsanitised (read-only escape).
22. publish-dashboard.py:588 — comment says stdout; prints go to stderr.
23. env-fingerprint.sh:53 — salt `efn-envcmp-2026-08-22` hardcoded in a public repo and output includes exact value length: a 7-digit value recovered from its 12-hex fingerprint by sweep. The header understates this: with the salt public the output is a value oracle. CONFIRMED.
24. zotero.py:70 — `file:///{ZOTERO_DB}?immutable=1` four slashes vs three in the importer (:1435); path not URL-encoded (`#`/`?` in ZOTERO_DATA_DIR truncates).

## Cross-file
1. env-fingerprint.sh:88-92 strips quotes and accepts `export`; the loader (importer 470-477) does neither — the tool built to prove two machines agree certifies a file the consumer cannot read. CONFIRMED on one synthetic file.
2. Two DOI normalisers; the write path uses the weaker (critical 1). Within zotero.py, SQL REPLACE strips `doi:` anywhere, Python strips only a prefix.
3. Three Retry-After parsers: _http_retry (seconds only, clamp [1,30]); lit-search._parse_retry_after:283-311 (also HTTP-date, clamp [backoff,60]); lit-scout-zotero-import.py:287-345 a hand copy of the lit-search pair.
4. build_zotero_item strips HTML from the abstract, not the title; _strip_html applied only to notes.

## Verified correct
_openai_key.py leaks nothing (suffix names only; hostname match as documented). env-fingerprint.sh emits key, digest, length only. No script writes Zotero SQLite; every connection immutable=1. publish-dashboard.publish:501-538 retries only canvas_editing_locked, never after a network timeout. _http_retry sets timeout on every attempt, bounded total wait, HTTPError before URLError. lit-search._safe_get catches JSONDecodeError (HTML rate-limit body degrades). _openalex_paginate terminates on a non-advancing cursor. sync-symlinks prune_stale_symlinks gates on -L and rms the link only; ln -sfn correct. compose-global-claude-md.sh atomic (mktemp + mv, chmod 0644), Claude-owned output only, never touches ~/.codex, AGENTS.md, gpt-hub (ownership.toml:80-112). No US spellings.

## Answers
1. External calls: lit-search → crossref, semanticscholar, openalex, no gate (every subcommand hits the network by default). importer → CrossRef→DataCite→OpenAlex on the default dry-run path; api.zotero.org only behind --live. add-doi same chain; writes behind --live. zotero.py local SQLite only. publish-dashboard → slack.com only behind --publish/--create AND SLACK_BOT_TOKEN AND canvas id. review-paper-prepass no network; aspell/texcount; shell=True only behind --build-cmd. ollama-endpoint.sh curl 192.168.1.150:11434 then 127.0.0.1, 3 s, no gate. syncthing-health docker inspect/exec locally and via ssh BatchMode to hosts in data/config/syncthing-expected.json (--local-only skips). syncthing-bind-heal local docker only. sync-symlinks: git submodule update, pip install. No rsync, no systemctl.
2. Zotero writes: importer :1283 create_collections, :1565 create_items; add-doi :206 create_items; sync-to-zotero :408 notes (out of tranche). Both default dry-run, --live required, no interactive confirmation. Duplicate prevention = find_existing_by_doi (broken for URL-wrapped) + importer's workspace manifest. Collection protection = ensure_subcollection (single page).
3. .env readers: importer load_env (also add-doi via load_importer) and env-fingerprint's Python heredoc; neither shell-sources. No value reaches stdout/log/report/argv. Two values reach a URL: OPENALEX_API_KEY as api_key query parameter (lit-search.py:242, host-gated, _safe_get logs the bare url). S2_API_KEY on the wire to wrong hosts (medium 3); Slack token on redirect (medium 7).
4. publish-dashboard publishes to a Slack canvas (workspace-scoped, bot-owned), content entirely from data/ (FOCUS slot names verbatim, counts, anomalies, data commit sha + dirty marker) — by design; gate --publish + token + --canvas-id; no prompt.
5. sync-symlinks deletes only symlinks under ~/.claude/{commands,skills,agents,output-styles} pointing into the repo whose source is gone; riskier acts are submodule update and pip --upgrade; no dry-run (only --quiet). compose overwrites exactly ~/.claude/CLAUDE.md atomically; --dry-run exact spelling only.
6. No reboot/unmount anywhere. One restart: syncthing-bind-heal.sh:57 `docker compose up -d --force-recreate`, strictly local, gated on compose file, docker, and config/cert.pem visible. syncthing-health reaches rpi-server and sapphire over ssh with reads only.
7. __file__-derived: add-doi:52 (locates the importer), SCRIPT_DIR/PA_DIR in the shell scripts. $HOME-derived: zotero.py:34 (overridable), importer 95-96 (ZOTERO_SQLITE, ENV_PATH not overridable), publish-dashboard.py:66 (PA_DIR + sys.path insert of the LIVE hooks dir: a sandbox copy still reads live data/tasks and runs git -C ~/personal-assistant/data), compose:30 TARGET, syncthing-health 53-54. No default writer targets the live data/ submodule; the isolation defect is compose (medium 11).
