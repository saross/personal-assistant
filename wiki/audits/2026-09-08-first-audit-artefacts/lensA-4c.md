# Lens A (correctness) — tranche 4: session archive pipeline — HEAD b4d0c57

## Critical
1. check-archive-drift.py:66 (MIN_CONTENT_CHARS = 4_000 prose) vs bulk-archive.py:2286-2298 (discover default --min-turns 5, --min-content-tokens 0 → is_trivial_session by turns): a 2-turn 28 KB session is substantive to the gate, trivial to the archiver at the flags the gate's remediation line (:188) tells you to run → permanent gate. CONFIRMED (drift exit 1, count 1; discover "0 to archive, 2 skipped (trivial)"; only --min-content-tokens 1000 finds it).
2. bulk-archive.py:479 `archived_ids |= catalogued` three lines after logging "%d session ids in CATALOG.json have no metadata on disk" — contradicts infrastructure-reference.md:191-193 ("never use it as a dedup key") and its own docstring (:326-332). A ghost catalogue entry suppresses archiving; drift check (reads metas only, :144) keeps flagging it → second permanent gate. CONFIRMED.
3. bulk-archive.py — no completeness guard: zero mtime/grace/size-stability; size_bytes at discovery (:564) never re-checked at archive (:1091); check-archive-drift has GRACE_HOURS = 48, the archiver nothing; once archived the id is in archived_ids (:516) forever; cmd_verify (:2205-2227) checks existence + meta parse only; validate-session-metadata never opens a transcript. A truncated transcript archived during a live session becomes canonical and every check reports clean. CONFIRMED (absence exhaustive).
4. backfill-summaries.py:729-732 — no flags → main → run_sync → generate_summaries → client.messages.create (:312); --batch-api → batches.create (:466). No estimate, no input(). Every sibling gates (bulk-archive.py:2032, reprocess-sessions.py:687, extraction-prompt-spotcheck.py:260). Violates the API Call Review Gate. CONFIRMED (read, not executed).
5. backfill-summaries.py:275-285 — _apply_summaries_under_lock writes rec["summary"] for ANY id in the model's reply found in the canonical; batch_index_map (:486) never read back. A hallucinated id overwrites an unrelated memory's summary. CONFIRMED (synthetic reply with one out-of-batch id: applied 2, victim replaced).

## Medium
6. reprocess-sessions.py:211 vs :520 — selection counts source == "extraction", writer stamps "reprocessing": not idempotent, re-spends every run; deterministic ids → byte-identical duplicate rows. CONFIRMED.
7. reprocess-sessions.py:640 custom_id = f"reprocess-{session_id[:8]}-c{chunk}" — two sessions sharing an 8-hex prefix collide (wrong attribution or whole-batch rejection). CONFIRMED (constructed).
8. reprocess-sessions.py:769-773 — apply BATCH_ID never checks state["batch_id"] == args.batch_id; an older batch applied after a newer submit maps through the wrong request_map. SUSPECTED.
9. normalise-archive-storage.py:166-167 — raw.unlink() precedes _repoint; failure never self-heals (run 1: raw deleted, gz written, meta still jsonl_path session.jsonl, exit 1; run 2: already-canonical, exit 0, still broken). _repoint (:185) is a bare write_text — a crash truncates the meta, dropping the session from archived ids and re-arming both gates. CONFIRMED.
10. bulk-archive.py:288-291 — layout probe: a live store with no top-level *.jsonl at scan time is read as a merged snapshot; session-UUID dirs become project keys, no warning. CONFIRMED.
11. reprocess-sessions.py:261-336 — no isMeta/isSidechain filter (0 vs 16 in the hook); docstring (:6) claims the same formatting as the hook; harness reminders and subagent turns go to Haiku and can become memories. CONFIRMED (read).
12. data/logs/bulk-archive-progress.json is git-tracked (95 tracked .json under logs; data/.gitignore covers *.log and *.jsonl only); cmd_archive honours already_done/already_failed unconditionally (:1030-1038) with no re-verification or expiry → a checkpoint synced from the other machine makes this machine skip sessions it never archived, against a per-machine store (:268-280). CONFIRMED.
13. bulk-archive.py:2152-2159 — _enrich_apply replaces auto_generated wholesale (drops three_ps from the Terra path; compare _write_enriched_meta:1886 which merges and tmp+replaces) and writes non-atomically. SUSPECTED.
14. bulk-archive.py:737-748 — archive_subagents writes straight to dest_gz, no temp, no existence check; re-run overwrites a good archived subagent; interrupt leaves a truncated .gz. cmd_subagents (:982-994) does it correctly. SUSPECTED.
15. bulk-archive.py:452 before :454-456 — _make_token_counter runs before the toolkit is on sys.path: `discover --min-content-tokens 1000` → "No module named cc_session_toolkit", exit 1; the preferred floor is the one that breaks. CONFIRMED.
16. bulk-archive.py:2254 — CATALOGUE_FILE.write_text, no temp+rename, no lock; two verify --fix-catalogue runs race; a crash leaves truncated JSON and get_archived_session_ids at :472 is unguarded, so the next discover dies. SUSPECTED.
17. push-archives-to-r2.sh:67-70 — `set -a; . "$ENV_FILE"; set +a` shell-executes .env and exports every secret into rclone/df/grep's environment (not just the two R2 vars named at :31-33). Credentials never reach argv. Separately `rclone copy --s3-disable-checksum` (:129) resolves on size+modtime: a truncated canonical with a newer mtime overwrites the last offsite copy. SUSPECTED (never executed).
18. Single-slot batch state: bulk-archive.py:54 and reprocess-sessions.py:55 overwrite one *-batch-state.json per submit; a second submit before apply destroys the first's map. SUSPECTED.

## Low
19. bulk-archive.py:12 documents `archive [--resume]`; no such flag (:2311-2330). CONFIRMED.
20. setup_logging() (bulk-archive.py:113, reprocess-sessions.py:183, backfill-summaries.py:111) dies with FileExistsError on the dangling logs -> data/logs symlink when data/ is absent. CONFIRMED.
21. _scan_archives.py:57-58 globs only session.jsonl.gz + subagents/*.jsonl.gz while bulk-archive.py:2205-2208 still accepts session.jsonl. CONFIRMED.
22. validate-session-metadata.py:166-170 Path.home()/"Code"/(project) from manifest data then git -C; resolve_session_id.py:60 root / rel from CATALOG.json: unvalidated joins. SUSPECTED.
23. check-archive-drift.py:181 GATE_FILE.write_text non-atomic (a SessionStart read can see a partial gate); a UTF-8 BOM drops the first record from the substance count (:90). CONFIRMED.
24. extraction-prompt-spotcheck.py:51 TRANSCRIPT_GLOB "*/*.jsonl" includes flat agent-*.jsonl which every other script excludes. SUSPECTED.
25. Style: 3 lines >100 chars (extraction-prompt-spotcheck.py:252, push-archives-to-r2.sh:103-104); validate-session-metadata.py:444 main() and extraction-prompt-spotcheck.py:180/209 lack docstrings; bulk-archive.py:1000,1164,2172 f-strings without placeholders.
26. Privacy (private submodule, not public): 95 .json files git-tracked under data/logs/ incl. 80+ terra-enrich-responses/terra/<session-id>.json (LLM summaries of private transcripts); data/.gitignore ignores logs/*.log and logs/*.jsonl but not logs/*.json, contrary to its stated intent. CONFIRMED.
27. Worktree hazard: MEMORIES_FILE/TAG_VOCAB_FILE (reprocess-sessions.py:56-57, backfill-summaries.py:54) and _bulk_rewrite_guard.PA_ROOT are __file__-derived, so from a worktree these write a different memories.jsonl and take a different logs/daily-sync.lock — the guard does not serialise against the live hook. CONFIRMED.

## Cross-file
- Three "substantive" rules: drift ≥4,000 prose chars (isMeta/isCompactSummary excluded, 48 h grace); discover turns ≥5 (or len(text)//4 ≥ N with --min-content-tokens); enrich MIN_CONTENT_TOKENS = 1_000 unconditionally. No shared constant (findings 1, 2).
- Atomicity per author: tmp+replace in cmd_subagents:987-994, _write_enriched_meta:1897-1899, write_memories:228-237, normalise:145-147; bare write_text in _enrich_apply:2157, cmd_verify:2254, _repoint:185, check-archive-drift:181, archive_subagents:742.
- Store roles: everything defaults to ~/cc-archives; resolve_session_id.py:36 alone defaults to the rpi share and returns None when unmounted, never falling back to the local mirror.
- _sync_cursor, _sync_gate, resolve_session_id not imported by any tranche script. extraction-hook.py writes no archive (cc_session_toolkit.archive.archive_session from a different hook does).

## Verified correct
UK English (canceled is an SDK attribute); no shell interpolation of filenames; search-archives-safe.sh honours the 2026-06-21 lesson (flock -n, nice/ionice/timeout, cgroup degraded check, no tr, foreground); _scan_archives bounded (ring buffer, per-line truncation, BadGzipFile/EOFError caught); extraction-prompt-spotcheck dry-run by default, writes nothing; BOM/CRLF/partial line tolerated by every parser; normalise verifies a sha256 round-trip before unlink; write_memories tmp+fsync+rename with a line-count invariant; push-archives-to-r2 uses copy, never sync/--delete.

## Answers
1. Paths: bulk-archive R ~/.claude/projects, ~/cc-archives, ~/Code/cc-session-toolkit/src, .env, data/experiments/…/prompt.md; W ~/cc-archives, logs/{bulk-archive.log, -manifest.json, -progress.json, bulk-enrich-batch-state.json, terra-enrich-usage.json, terra-enrich-responses/terra/*.json}. check-archive-drift R ~/.claude/projects, ~/cc-archives; W ~/.cache/cc-archive-drift-gate. reprocess-sessions R ~/cc-archives, memories/*, .env; W memories.jsonl, tag-vocabulary.txt, logs/*. backfill-summaries R/W memories.jsonl, logs/*. validate-session-metadata R --responses-dir, --manifest, ~/cc-archives top level, arbitrary repos via git cat-file; W --report. normalise R/W ~/cc-archives (destructive). _scan_archives/search-archives-safe R ~/cc-archives; W stdout + ${TMPDIR:-/tmp}/cc-archive-search.lock. push-archives-to-r2 R rpi share, .env; W R2 + logs/r2-push.log. extraction-prompt-spotcheck R ~/.claude/projects, hooks; W ~/personal-assistant/reports/.
2. Yes and yes (finding 3).
3. Three rules, disagreeing (cross-file).
4. verify --fix-catalogue alone (:2251-2261), rebuilt from disk, unlocked, non-atomic.
5. normalise unlinks raw (verified round-trip, but before _repoint); archive_subagents overwrites; _enrich_apply overwrites meta and drops three_ps; R2 objects overwritten on size/mtime change. Nothing deletes an archived session transcript.
6. External: OpenAI Responses bulk-archive.py:1425 (enrich --provider terra, cost printed, input() unless --yes); Google GenAI :1767 (--fallback-gemini + invalid_prompt refusal); Anthropic Batch bulk-archive.py:2039/2084/2104 (input() at :2032), reprocess-sessions.py:694/728/773/787 (input() at :687); Anthropic Messages backfill-summaries.py:312 and batch :466 UNGATED (finding 4); extraction-prompt-spotcheck.py:204 gated by --run; rclone → R2 push-archives-to-r2.sh:141/147 gated by mount/remote preconditions and --dry-run; git cat-file local. No ssh/HTTP/Ollama.
7. __file__/BASH_SOURCE defaults: bulk-archive.py:41,48,221,1795; reprocess-sessions.py:39,51; backfill-summaries.py:41,52; push-archives-to-r2.sh:43-47; search-archives-safe.sh:83-84,121; _bulk_rewrite_guard.py:72-76 — all checkout-relative (finding 27 is the inverse risk); extraction-prompt-spotcheck.py:49 pins to ~/personal-assistant deliberately.
