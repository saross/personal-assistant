# First-audit artefacts

Working documents from the first repository audit (report:
`../2026-09-08-first-audit.md`). Copied here from the session scratchpad on
2026-09-09 so that a later session can resume the open rounds without the
original conversation.

- `audit-round2-brief.md` — the shared brief every fix agent reads first
  (safety constraints, code standards, commit rules, synthetic fixtures,
  deliverable).
- `audit-round4*-brief.md` — the per-round briefs (scope, files another
  round owns, the ordered fix list).
- `lensA-*.md` / `lensB-*.md` — the two fresh-context lens reports per
  tranche (correctness; test adequacy by mutation), with the finding IDs the
  report tables use. Line numbers are as of the commit named in each file.
- `round*-report.md` — each fix agent's disposition report, appended round
  by round.

Every fixture named in these documents is synthetic; no memory content,
transcript text, or credential appears here.
