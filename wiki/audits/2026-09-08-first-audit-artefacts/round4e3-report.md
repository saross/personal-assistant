# Round 4e-3 report (branch claude/audit-round4e-3; 6 commits 2b5318c..b817966)
M1 --resubmit (:1821) unlocks a second submission keeping the pending filter; --force keeps "everything again"; haiku_submit param renamed allow_resubmit; --resubmit off the Batch arm exit 2; reachable test: submit 3 → apply 2 → --resubmit → batch of 1, batch_001 in superseded_batches (4 tests). M2 three equality assertions on the recovery line. L1 submitted_at parsed and repeated; L2 both ids and the custom_id in the collision message; L3 a REAL defect: two n_fail += 1 on the empty-content branch (counted twice; refused write counted as failure AND kept) — fixed and pinned from both sides; L4 docstring (the suite's temp home); L5 coverage check names five known files.
Suite 3339 passed, 1 skipped, 3 deselected, exit 0.
Live risks: --force on the Batch arm now means "send everything again" (the old habit is the expensive one); --resubmit with a non-Batch provider exit 2; --resubmit with no gap exits 0 without a batch; empty-content failure counts were inflated ×2 in past runs; batch-state.json unchanged.
4e-4 (6 commits 6fc42b6..1b50d6e): M-a --manifest/--prompt optional at the parser (:1775), the three modes that need them check explicitly (exit 2 naming the mode); test_the_emitted_line_actually_runs shlex-splits the printed line into the real main() and reaches the apply path; fences for live/dry/rubric. M-b custom_id_to_session accumulates ({**previous, **new}); the superseded batch retrievable. Mutations: superseded chain after two top-ups; the main-level check pinned by timing (fires before the gate, no --yes); haiku_submit refuses on its own (the main pass-through is equivalent by construction — noted); three colliding sessions all reported; the empty-content diagnostic asserted verbatim. L-a threshold test; L-b the snapshot walks .git (only __pycache__ skipped; 0.04 s); L-c --resubmit with --haiku-apply exit 2. Commit message 45c4325 corrected (amended).
Suite 3366 passed, 1 skipped, 3 deselected, exit 0.
Live risks: --manifest/--prompt no longer enforced by argparse (same exit 2, different wording); batch-state.json's map grows across top-ups (read n_requests for the current batch); state files written before this change are not repaired (a superseded id may still skip — rebuild from the manifest); the snapshot walking .git could attribute a concurrent git operation in a shared checkout to the analyser test (message names the paths).
