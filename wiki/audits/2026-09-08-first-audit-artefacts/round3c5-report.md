# Round 3c-5 report (branch claude/audit-round3c-5; 4 commits a9de215..6066edf)
M1 corpus_lines_at reports presence separately: a parent without the corpus is unknown and excluded from the minimum; a merge none of whose parents holds it is refused as unjudgeable; the loop fails closed (exit 4, "the shrink could not be attributed") when the outer comparison saw a shrink no commit explains; 5 tests. L-a the sweep marker takes the six-character shape (collected by the next sweep). L-b two narrowing-clause tests. L-c stash_tracked_half_is_binary gates the wording (kept, compare by hand). L-d --no-renames on the path list and the diff; status_lines_for matches the `R old -> new` porcelain form. 11/12 mutations killed (the survivor is behaviourally equivalent and stated).
Found: `| grep -q` under pipefail — grep -q exits on first match, upstream dies of SIGPIPE, pipeline reports failure: BOTH bulk-trailer checks had that shape (a legitimate archive run refused on a race decided by message length); all three sites now read the text via has_bulk_rewrite_trailer; pinned as a source contract for the two push gates. stash_tracked_half_is_binary never declared its locals (caught by the test; never ran correctly).
Suite 2848 passed, 1 skipped, 3 deselected, exit 0 (clean copy, HOME pinned, --basetemp). Merge-base 87db26b, clean.
Live risks: two new exit-4 paths (merge parents lacking the corpus; an unattributable shrink — fires when HEAD publishes a shorter corpus than origin/main with no shortening commit in the range, i.e. a diverged local branch); the trailer fix may let a previously wedged Rewrite-Class: bulk commit publish on the next tick; the sweep deletes its own marker pattern; a binary stash fails with its own message in the parent half.
3c-6 (5 commits 469e104..35b24d2): L1 the lint scans the whole file (no grep -q on the far side of a pipe), no allow-list — the four bounded uses converted to here-strings/$( ); L2 binary line class + retiring a stale REFUSED line; L3 the writer's own orphan collected (mv stubbed); L4 an unmeasurable merge recorded, scan continues, decides only when nothing else can, named; L5 CONFIRMED: porcelain quotes paths with spaces — status_records reads --porcelain=v1 -z (rename paths as separate fields; arrow-parsing gone); L7 legacy sweepmark collected. 10/10 mutations killed. Self-caught: the lint's vacuity guard counted prose; now code lines only. Suite 3353 passed, 1 skipped, 3 deselected, exit 0. Live risks: entries touching paths with spaces can now be dropped when landed; the cc-archives mount probes rewritten (decide whether rsync/R2 run); L4 more permissive in one shape (an unmeasurable merge beside a trailered owner publishes, unreported); a second sweep pattern.
