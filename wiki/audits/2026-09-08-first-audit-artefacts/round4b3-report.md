# Round 4b-3 report (branch claude/audit-round4b-3; 6 commits 198bf7d..79911a4)
M-1 _soft_delete.py:52 _FALSE_TOKENS = PostgreSQL's set (false, f, no, n, off, 0); 0.0 via the numeric branch; normalise_flag() at :97 writes a real bool, used by sync_memory_edit.py:100 and sync-to-postgres.py:475; tests/test_soft_delete.py full truth table (not derived from the constants); both writers assert the singleton at the driver boundary; four mutations fail. M-2 search-sessions.py --query-stdin; recall.md:190 quoted heredoc; fallback rule single-quote and REFUSE a query with ', backtick, or $( ; test_hostile_text_reaches_the_query_verbatim (six shapes). L-1 tests/test_fake_pg.py view vs base table for the COUNT FILTER shape. L-7 _fake_pg.py:80 UnsupportedSQL (AssertionError) rejects writes, non-SELECTs, unimplemented clauses, unknown tables/columns/conditions/orderings; found a fourth silent mis-answer (ORDER BY looked for inside the WHERE text) — clauses now peeled in SQL order. L-2 roundtrip_home asserts; mkdtemp cannot supply the root (its alphabet includes "_"), so a uuid-hex dir under /tmp via mkdir. L-3 test_prioritisation_actually_reorders. L-5 one stderr line for a missing/unparseable catalogue. L-6, L-8 recorded.
Suite 2911 passed, 1 skipped, 3 deselected, exit 0 (clean archive, HOME pinned, STRICT, --basetemp).
Live risks: records with is_active "no"/"off"/"f"/"n" disappear from JSONL paths on merge (coordinator grep: see count above); the two writers now write normalised booleans so previously failing rows reconcile on the next cron sync; --query-stdin additive; resolve_session_id newly chatty on stderr for a missing catalogue; the hardened fake raises on unsupported SQL.
