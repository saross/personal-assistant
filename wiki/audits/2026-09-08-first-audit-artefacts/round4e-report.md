# Round 4e fix agent report (branch claude/audit-round4e; 17 commits deebe95..7073d19)
AS1 bake-off-metadata.py:1246,1301 SESSIONS_SPAN_RE + validate_rubric_template (refuse a populated template, exit 2, write neither); AS2/AST5/AST8 :1265 blind_order (every arm labelled, seeded full permutation, byte-identical keys); AS3 :144-145 3.00/15.00 with a review tripwire; AS4/AST3 resample :78,560,729 PA_DIR from __file__, write_json_atomic, argparse (--out required, no clobber without --force, --dry-run, --seed, roots from $HOME); AS5/AST13 :712 parse_as_of, one clock reading; AS6 :266 build_custom_id full id; AS7/AST12 :1514,1546 gate prints model/mode/count/cost, closed stdin refuses cleanly, --yes proceeds; AS8 retrieval banner; AS9 :504,537,556,583 atomic writes, complete responses never overwritten, resume by default, usage merged; AS10/AST10 analyse-wiki-vocabulary.py:227,315 empty/undated/odd tags; AS11 v2.md:692 §11; AS12 v2.md:803,:810 21.45 and 1.605 (coordinator verified against data/style-corpus/phase1-results-clean.json: see below); AS13 v2.md:577-612 durable manifest; AS14 four style-analyser path defaults from __file__; AS15 variant (hint, docstring, ImportError→venv message; shebang unchanged — all 75 scripts use /usr/bin/env python3); AS17/18 six arms, thinking-level claim, service_tier recorded; AS19; AS20/AST7 SHORTFALL lines; AS21; AS22; AS23/AST11 normalise_tag NFC→strip→lower; AS24/AST6 empty manifest clean exit; v2 reconciliation contradiction; AST1 tree-untouched snapshot test; AST2 SOURCE_PREFERENCE + deduplicate_candidates (archive preferred); AST9 load_env only on billed/retrieval paths. Not fixed: AST4 (conftest, another round's), AST14 (out-of-repo extractor), AS16, AS25 recorded.
NEW: bake-off-metadata.py:634-660 haiku_submit has no resume for the Batch arm — re-running creates a second billed batch and overwrites batch-state.json (left: "refuse if batch-state.json exists unless --force" is a behaviour decision); self-caught 7073d19 (the AS9 skip left the gate quoting the whole manifest; pending filter now runs first); resample:44 unused asdict; SUSPECTED blind-key sentinel "_redacted_errors" shares the session-id namespace.
Provenance proposal: <out>/<provider>/_run.json with provider, model_id, service_tier, max_output_tokens, prompt_sha256, manifest_sha256, manifest_path, script_commit, generated_at; prompt_sha256/pool_sha256 in the manifest header.
Suite 2571 passed, 3 deselected, exit 0 (117 new tests).
Live risks: resampler requires --out and refuses to replace without --force; pools from $HOME; --build-rubric refuses a populated rubric; live runs resume by default (--force for all); sonnet-5 estimates rise by half; analyse-wiki-vocabulary returns exit codes 1/2; style-analyser phase scripts resolve data/style-corpus from the repo root.
