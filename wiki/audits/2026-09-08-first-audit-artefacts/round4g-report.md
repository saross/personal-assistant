# Round 4g fix agent report (branch claude/audit-round4g; 11 commits 28e72f0..b954f7a)
ST1/ST2 efficacy_score_judges.py:70,110,383 typed unusable record, tolerant parse, empty/duplicate/unknown-id errors; ST13/STT-M6 :162,208 pairs collapsed, exact binomial, prose from the key. ST3/ST31/STT-M5 efficacy_build_judge_tasks.py:68,106,143 key in sibling judge-key/, seeded shuffle + randomised side, one file per unordered pair, rmtree refused. ST4/STT6/ST28 phase3_guide_verifier.py:121 feature→metric map, check 4b per-1k, verify_guide:668 UNVERIFIED rows + --allow-unverified, §3.4 mapped. ST6/ST7/ST29/STT-M1/STT-M2 :239 structural sub-cluster parse, :255 key shape needs letters+digits, _match_status:329 one row per branch, sorted iteration. ST5/ST8/ST9 phase1_pipeline.py:330 hapax over types (hapax_per_token:348 keeps the old), :461 passive presence per sentence (per-verb kept), :459 shared alphabetic denominator. ST15/18/19/20/21/23/STT5 :127 heading marker or bare label, process_paper(strip_refs=…), :311 MATTR None + flag, NFC, :802 whole-word exclusion, :924 empty corpus exit 2. ST25/26 phase3_promotion.py:142 mismatch raises, :74,229 CONTINUOUS_METRICS/presence_rule_applies, :441 n_occ=0 prints 0. ST10/L5 efficacy_build_reference windows over stripped text, floor post-strip, empty reference exit 2, phase5 lazily imported. ST11/12/STT3 phase5_evaluator.py:1076-1090 n−1 fit (integration-marked test could not run), :1114 style_support.sanity_verdict. ST14/STT2/L1-4 validators default to data/style-corpus/extracted/<key>/body.md, --corpus-dir/--keys, guarded reads, exit 1 on empty sample. ST22/23 phase4 paragraph-wise segmentation. ST27 efficacy_analyse paired_rows. ST32/STT-M7 extract_corpus.py:875 manifest inside --output-dir, --keys on EXCLUDED refused (exit 3), lazy import. ST16/17/STT-M4 prompts citation shapes, fence/indent-safe tidy-up, guide read first. Atomic writes + --dry-run on all 12 writers + provenance via new style_support.py (203 lines). 239 new tests in 10 files; numpy/spaCy halves importorskip + integration.
Not fixed: ST24 (stop-list decision), ST30, ST33, STT7 (proposals), STT-L6 (outside), S2/H1.
Proposals: requirements numpy>=1.26, scipy>=1.11, scikit-learn>=1.4, spacy>=3.8,<3.9, en_core_web_sm==3.8.0 (GitHub wheel); length-matched gate for ST30; generator sidecar {model, prompt_sha256, condition, generated_at} per passage.
NEW: v2.md:772 passive 0.28→0.31 is per-verb, contradicts §5 :252 (round 4e's file); efficacy_build_prompts build_manifest relative_to raised for out-of-repo guides (fixed); phase5_evaluator.py:707 advisory bimodal ranges still from the full per_paper (SUSPECTED); extract_corpus.py:139-142 comment vs pattern (Acknowledgements); :666 stale extraction-error.txt never removed; :589 literal "## Abstract" (SUSPECTED); phase4:246 file handle leak. Environment: /tmp inodes exhausted mid-round (worked around with TMPDIR=/var/tmp/pa-audit-4g — delete when merged).
Suite 2692 passed, 16 deselected, exit 0. Not pushed. Merge note: conflicts with round 4e (now on main) in phase4_exemplar_scorer.py (and possibly the argparse blocks of phase3_promotion/phase3_guide_verifier).
Live risks: every published figure derived from hapax, passive, nominalisation, MATTR-on-short-text or announcement colons changes definition — phase 1 must re-run and the guide re-derive; judge task layout changes (existing judgments.jsonl still scores via a fallback key with a warning); the verifier exits 1 on unmapped claims unless --allow-unverified; phase5 --validate can exit non-zero for two reasons; corpus-manifest.json moves; validators exit 1 on an empty sample; JSON outputs gain provenance + trailing newline.
4g-2 (13 commits 53d28eb..36679ed): 1 style_support.impute_missing_features (corpus mean, named in imputed_features and the report) at phase5_evaluator.py:706-716; 2 unordered_pair_key returns None when it cannot identify an entry, main refuses naming the pair ids, orders_per_pair > 2 fatal, legacy key with all three fields still scores; 3 style_support.PHASE1_RESULTS_DEFAULT shared, corpus-local file wins when present, missing file exit 2; 4 metric_schema version 2 stamped by phase 1 (and provenance), refused when missing/older by phase3_promotion, phase3_guide_verifier, phase5_evaluator, efficacy_build_reference, efficacy_score; promotion and build_reference carry the stamp into their outputs; phase4 consumes no phase-1 results; 5 three doc lines repointed, the old copy declared a pre-2026-09-09 artefact; 6 copyfile → atomic writer; 7 _PER_1K_RE accepts comma/space/no separator, per 1k, /1k, integers; 8 check 5 decides against the KNOWN key set; 9 git_state requires the file tracked by the repo it records, reports dirty; 10 key under <experiment>/private/judge-key/ with README_DO_NOT_SHARE.md, containment refused both directions, --migrate-key (not run live); 11 held-out sample reads a phase-1 copy without its own row; the Gate cell renders "—" (aggregate targets cannot be held out); 12 --validate provenance section; missing held-out body.md is a FAIL; 13 build_reference no longer strips clean body.md; _REF_HEADER_RE IGNORECASE + CRLF. All named mutations killed. Lows done (stale extraction-error.txt cleared on success; "# Abstract" any level; Acknowledgements comment corrected not the pattern; handle leak; extra cannot clobber; bare fence not a judgement).
Suite 3262 passed, 5 skipped, 19 deselected, exit 0 (phase-5 integration tests written but not executed — no numpy).
Live risks: every consumer refuses the live phase-1 results (exit 2) until phase 1 is re-run (--clean-corpus, then promotion, then downstream) — the interlock working; --validate stricter twice (missing held-out body.md FAILs; the held-out Gate cell "—"); the judge key moves to private/ (scorer still reads a legacy key with a warning; --migrate-key not run); validate_announce_colon exits 2 without results; the stale data/style-corpus/corpus-manifest.json should be deleted by the operator (submodule). Flagged: check 5 emits a PASS row per key mention; two extract_corpus items remain (case-sensitive "Abstract" in the QA flag; stale-error clearing before the bundle writes).
