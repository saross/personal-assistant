# Round 4d-3 report (branch claude/audit-round4d-3; 6 commits 732bc4e..cdbe27a)
M1+M2 sync-symlinks.sh:271-291: a linked worktree (its .git is a FILE) skips data/ outright; a non-empty data/ in a non-worktree reports what to clear instead of attempting an init that cannot succeed (verified: git refuses a non-empty destination); init only for a fresh clone (data/ absent or truly empty); TestSubmoduleUpdateIsGated (6) incl. test_allow_worktree_completes_all_eight_steps. L1 header says first-wins (the warning test reads the header too). L4 syncthing-health.sh:78-88 `*)` warns with a usage line, exit 0. grep -qxF pinned with pa-data vs pa-data-archive both directions and pa.data vs paXdata. zotero trim fixture padded with tab, CR, LF, space AND NBSP; char(160) added (a real behaviour change: Python strips U+00A0, SQLite TRIM did not). exc.start pinned against two offsets. "Submodule ready." via did_verbose. llm-use-inventory :368 already covered; :367 (archive root resolve) now pinned. Trim docstring states what is not covered. resolve_path readlink -f on the leaf (a symlink FILE naming the live CLAUDE.md refused); correction: mv replaces the link rather than following it (the former destination untouched; the link destroyed) — comment and test state the real mechanism. Reversed a 4d-2 test that asserted an init that cannot succeed (now test_a_non_empty_data_reports_rather_than_attempting_the_init).
Suite 3312 passed, 1 skipped, 3 deselected, exit 0.
Live risks: the live checkout unchanged (submodule initialised → left alone); --allow-worktree now relinks every ~/.claude symlink at the worktree as advertised; a clone with a stray file in data/ warns and continues instead of aborting; NBSP-padded stored DOIs now match (importer withholds as duplicate); syncthing-health warns on unknown arguments.
4d-4 (7 commits 3986a48..f759d9f): M1 sync-symlinks.sh:311 drops the ALLOW_WORKTREE disjunct (the flag is consent, IS_WORKTREE the evidence). M2 DATA_REMEDY named once (the only remedy that works); the composer keeps init advice only when data/ is absent; step 1 stops with exit 1 and nothing relinked on an ordinary clone lacking data/global-claude-md/local.md; a worktree completes 2-6 and skips 7 with a message; --dry-run exempt (8 tests). M3 sync_sandbox creates .git as a directory; _make_worktree() replaces it with a gitdir: file. L1 relative symlink + symlinked ~/.claude directory tests. L2 comment counts 24 divergent characters; U+202F and U+2007 added to the trim set. L3 did_verbose both halves. L4 --project . L5 `-e || -L` (dangling symlink refused). L6 early return pinned by consequence. Scope note: on a REAL worktree --allow-worktree would also have died at step 7 (no local.md) — now the skip-step-7 branch.
Suite 3702 passed, 1 skipped, 3 deselected, exit 0.
Live risks: sync-symlinks can exit 1 at step 1 on a clone with unusable data/ (live checkout unchanged); --allow-worktree on a real worktree skips step 7 — a deliberately mixed state (CLAUDE.md from the live checkout, symlinks at the worktree); two more padding characters count as duplicates; two more target spellings refused by the composer.
