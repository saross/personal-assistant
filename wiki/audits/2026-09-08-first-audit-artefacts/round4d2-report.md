# Round 4d-2 report (branch claude/audit-round4d; 8 commits 2ef677e..875a435)
C1 syncthing-health.sh:355 passes "$EXPECTED_FILE" "$expected_id" "$threshold_h"; :322 float(sys.argv[3]); TestPeerAbsenceAlertActuallyFires (4) drives check H end to end through a dispatching stub docker; $FOLDER_ID reaches the inner sh -c as $1, percent-encoded and quoted (:216); grep -qxF; 5 mutations killed. C2 env-fingerprint.sh:91 `python3 - "${ENV_FILE}"`, salt via os.environ; whitespace trimmed and whitespace-only refused (:69-80); header + wiki/docs/env-cross-machine-reference.md pipe the salt down the remote shell's stdin; TestTheSaltNeverReachesArgv (4). compose --target bypass: :100-140 guard keyed on the resolved target's identity; unusable live root refuses; TestComposerTargetIdentity (6); directory target refused before any temp (:87). sync-symlinks live-root guard before step 1 (:73-96), --dry-run exempt, --allow-worktree threaded to the composer as --allow-foreign-root (3 tests). mkdir -m 700 (:199). E10 gate via `git submodule status -- data` and the `-` prefix (:180-192), 5 tests incl. stray file. env-fingerprint doc: first assignment wins (pinned against the loader); strict UTF-8, invalid byte reported by offset, exit 3 (4 tests). no_network_guard.py deleted (nine files rely on conftest). Fixtures: padded + trashed DOI items exposed that SQLite TRIM strips spaces only → shared _SQL_TRIMMED_DOI in zotero.py:406; add-doi immutable open pinned; llm-use-inventory ../../etc and symlink refused; prepass manuscript in sections/; vacuous __module__ replaced; sapphire address read from the script. --dry-run tense.
DEVIATION needing a ruling: absent live root in the composer is a WARNING, not a refusal, because tests/test_glue_scripts.py::TestComposeGlobalClaudeMdIsAtomic (round 4c's file) runs with a pinned HOME and no ~/personal-assistant; a refusal would make the atomicity test pass for the wrong reason. Unreachable in production. Ask round 4c's owner to add an explicit opt-in to that fixture, then flip to a refusal.
NEW: SQLite TRIM spaces-only divergence (fixed); --allow-worktree alone would have reproduced the half-migrated state (threaded); SUSPECTED syncthing-health.sh:63 no `*)` branch (typo'd flag ignored; kept for "always exits 0").
Suite 2687 passed, 1 skipped, 3 deselected, exit 0.
Live risks: sync-symlinks refuses from anywhere but ~/personal-assistant (exit 2; daily-sync:2117 and setup.sh:26 unaffected); compose refuses the live target from a foreign checkout even with --target (escape --allow-foreign-root); env-fingerprint exits 3 on an invalid byte; the salt recipe changed (stdin form; trimmed salt changes digests only if it carried whitespace); ~/.claude created 0700; the submodule gate consults git.
