# Round 4f fix agent report (branch claude/audit-round4f; 8 commits d98c3c5..b120a39)
AN1 anchor_verify.py:150-166 --literal-pathspecs, :404-407 magic gate — TestPathspecMagicNeverVerifies. AN11 :81-99 _under_repo at :249,:263. AN12 :349-364 _MIN_COMMIT_HEX = 7. AN3 :135-190, 205-280, 316-345 transient → pending; bind_confidence(current=) :637 — TestTransientFailureIsPending (12). AN7 triage_anchors.broad_repo_set raises on empty; drift-sweep.py:87-127 min_repos + last_repo_count. AN18 drift-sweep.py:236-258 lost trend row = failed run. AN2 anchor_verify.TrackedPath/SuffixMatch/unique_suffix_match(project_repos=); triage_anchors.build_basename_index; recover_anchors.project_repos_for + --allow-cross-repo — TestRecoveryIsScopedToTheMemorysProject. ANT1/2 + AN9 + AN4 tests/_fake_audit_pg.py (SQL-evaluating, write-refusing); audit-postgres-sync fingerprints, strict orphans, duplicate ids, read-only/rollback, SchemaVersionError raised — 37 tests. ANT3/4 + AN5/6/8/14/16/17/M-c/AN10 memory-health-report — 51 tests. ANT5 + Md/Me/Mf/L1/L5/L7/Mh broad_repo_set delegates to project_id.repo_set + PA_DIR; run_sweep unstubbed; triage main tree snapshot. AN13 memory-health-report only (surfacing_stats untouched).
NEW: tests/_fake_pg.py (round 4b) collides by name — renamed to _fake_audit_pg.py; unify later preserving the write refusal. anchor_verify._VERIFIERS binds at import (monkeypatch trap). SUSPECTED identical commit hashes for same-second same-content throwaway repos. Environment ENOSPC window.
Recorded: AN15 (proposed rule: strip the longest discovered-repo prefix, then project-scoped suffix match; refuse outside every repo), ANT-Mg conftest additions (data/reports/, data/notes/, archive partitions, quarantine file), ANT-L6.
Suite 2560 passed, 3 deselected, exit 0.
Live risks: /memory-health FAILs if quarantine-postgres-drops.jsonl is absent or unreadable; 15 glob-ref anchors re-verify false and are now malformed (recover_anchors would strip them); the next drift sweep may exit 2 with no trend row if >10 % pending, discovery empty, or fewer repos than the last logged run; audit-postgres-sync will likely exit 1 on the live store (content divergence and PG orphans now count; every /update, /forget, recover --apply record diverges under ON CONFLICT DO NOTHING); recover --apply recovers fewer refs (cross-repo refused by default).
4f-2: memory-health-report quarantine_state() → (count, state) with absent → 0 "never written on this machine" PASS; read → real count; unreadable (exists, read_quarantine_entries None, or an OSError on the existence check) → UNKNOWN FAIL; 6 tests; commands/memory-health.md corrected. Commit 6246c2e. Suite 3062 passed, exit 0. Live-risk 1 withdrawn.
4f-3 (14 commits 1e4d509..ed1114e): C1 triage_anchors.broad_repo_set_detail (discovery-only count, PA_DIR augmentation excluded) + RepoSetShrunk; drift-sweep --min-repos, --no-log drops the floor, refusal names both numbers and the override. M1 build_report catches RepoSetUnavailable → "[F] Tier C — skipped". M2 imports _soft_delete.is_active. M3 wording pinned. M4 SET LOCAL statement_timeout first on both connections; value asserted. M5 fake view is_active is True; assertion 1. M6 repository-level failures mark the repo unusable (warn once, excluded); only ref-level timeouts yield pending. L1 _resolves_inside on the stat hit (internal symlinks still true). L2 pending returns the record's own high/low case-folded; medium only with no prior rating. L3 allow_union_fallback widens only when the project holds no candidate. L4 recover_anchors.main routes through broad_repo_set, exit 2 before planning. L5 dead code removed, malformed_anchor_fields rendered. L6 clean includes only_in_postgres (unsynced tail exempt). L7 [G] membership against active ids. L8 shape gate ≥ 4 hex; verify_commit requires a unique hit below 7 else pending (never stripped). Ten live-corpus values in test_anchor_verify.py scrubbed. All listed mutations fail.
Suite 3108 passed, 1 skipped, 3 deselected, exit 0.
Live risks: both the audit and /memory-health fail on a Postgres-only row; the first sweep after merge refuses once if the last row came from a worktree (prints --min-repos N); a broken repository is dropped after one stderr warning (anchors there read false, not pending); the symlink guard may flip live anchors whose path leaves the repo through a symlink; 4-6 hex commit anchors become pending; --allow-cross-repo now rewrites refs.
