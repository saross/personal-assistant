# RDA IG application documents — relocated to the private archive

The two application `.docx` files formerly here (`RDA_IG_Statement_of_Work.docx`,
`RDA_IG_Summary_and_Description.docx`) contained the 27-member table with
email addresses and were removed from this public repository on 2026-08-03
(privacy-hygiene decision, captured 2026-07-27). Canonical copies — plus the
Markdown sources and revision history — live in the private data submodule at
`data/archive/rda-ig-application-2026-07/`.

Residual-risk note, accepted at the decision: **git history retains the
removed files.** Rewriting history was considered and declined (the emails
also circulate in RDA submissions; the marginal exposure does not justify a
history rewrite on a multi-machine repo).
